DATABASE_URL="mysql://root:Arituch%40191146@127.0.0.1:3306/pet_pathfinder?connection_limit=5"

PORT=4000

JWT_SECRET="change-me-please"
# URL ของ frontend สำหรับสร้างลิงก์รีเซ็ตรหัสผ่านที่ส่งไปในอีเมล
# ใช้โดเมน production ของหน้าเว็บที่ผู้ใช้เปิดจริง
FRONTEND_BASE_URL="https://pup-peturgentpath.com"

# ถ้าต้องการทดสอบบนเครื่องตัวเองแบบ local dev ให้เปลี่ยนกลับเป็น
# FRONTEND_BASE_URL="http://127.0.0.1:5173"

# ---------------- SMTP CONFIG สำหรับส่งอีเมลจริง ----------------

# ตัวอย่างถ้าใช้ Gmail SMTP (ต้องใช้ App Password)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="arituch.y@gmail.com"
SMTP_PASS="ahvg vhtp cseu cplc"   # ไม่ใช่รหัสผ่าน Gmail ปกติ ต้องเป็น App Password
MAIL_FROM="PUP Support <your-real-gmail@gmail.com>"